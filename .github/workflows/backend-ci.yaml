name: Backend Continuous Integration
on:
  pull_request: { branches: [ "main" ] }
  workflow_dispatch:
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: ./backend/package-lock.json
      - run: npm install --prefix backend
      - run: npm run lint --prefix backend
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: ./backend/package-lock.json
      - run: npm install --prefix backend
      - run: npm test --prefix backend -- --ci --watchAll=false
  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: ./backend/package-lock.json
      - run: npm install --prefix backend
      - run: docker build -t movie-backend-ci:${{ github.sha }} ./backend
